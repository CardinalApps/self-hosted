diff a/servers/media/src/utils/transformers.ts b/servers/media/src/utils/transformers.ts	(rejected hunks)
@@ -21,6 +21,9 @@ export const toArrayOfStrings = ({ value }) => {
   }
 
   if (typeof value === 'string') {
+    if (!value.length) {
+      return []
+    }
     if (value.includes(',')) {
       return value.split(',')
     } else {
