diff a/servers/media/src/modules/rbac/rbac.service.ts b/servers/media/src/modules/rbac/rbac.service.ts	(rejected hunks)
@@ -18,7 +18,7 @@ export class RBACService {
   /**
    * Gets all assignments for a single user.
    */
-  async getAndCountRoleAssignments(): Promise<RoleAssignment[]> {
+  async getAndCountAllRoleAssignments(): Promise<RoleAssignment[]> {
     let found = null
 
     try {
@@ -34,6 +34,25 @@ export class RBACService {
     return found
   }
 
+  /**
+   * Gets all assignments for a single role.
+   */
+  async getRoleAssignments(role: RoleNames): Promise<RoleAssignment[]> {
+    try {
+      const found = await this.roleAssignmentRepository.find({
+        where: {
+          role,
+        },
+        relations: {
+          user: true,
+        },
+      })
+      return found
+    } catch (error) {
+      Logger.error(error, 'RBAC')
+    }
+  }
+
   /**
    * Gets all assignments for a single user.
    */
