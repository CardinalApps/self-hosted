diff a/servers/media/src/modules/rbac/rbac.service.ts b/servers/media/src/modules/rbac/rbac.service.ts	(rejected hunks)
@@ -91,11 +91,6 @@ export class RBACService {
    */
   async assignRole(roleName: RoleNames, users: User[], queryRunner?: QueryRunner): Promise<RoleAssignment[]> {
     const assignments = []
-    const role = getRole(roleName)
-
-    if (!role.assignable) {
-      throw new Error(`The role "${roleName}" is not assignable.`)
-    }
 
     for (const user of users) {
       const assignment = await this.hasRole(user, roleName)
