diff a/servers/media/src/modules/rbac/rbac.controller.ts b/servers/media/src/modules/rbac/rbac.controller.ts	(rejected hunks)
@@ -7,39 +7,43 @@ import {
   Post,
   Delete,
   Param,
+  Query,
 } from '@nestjs/common'
 import {
   ApiTags,
   ApiSecurity,
   ApiOperation,
 } from '@nestjs/swagger'
+import { RoleNames } from '@cardinalapps/access-control/dist/cjs'
 
 import { AuthGuard } from '../../guards/auth.guard'
 import { ApiSecurityTypes } from '../../guards/types'
 
 import { RBACService } from './rbac.service'
-import { CurrentUser } from '../../decorators/CurrentUser.decorator'
 import { RoleAssignment } from './role-assignment.entity'
 import { AssignRolesDto } from './dtos/AssignRoles.dto'
 import { RevokeRolesDto } from './dtos/RevokeRoles.dto'
+
 import { User } from '../user/user.entity'
+import { UserService } from '../user/user.service'
 
-@Controller()
 @ApiTags('Role Assignments')
+@Controller('/roles')
 export class RBACController {
   constructor(
     private readonly rbacService: RBACService,
+    private readonly userService: UserService,
   ) {}
 
   /**
-   * Returns the role assignments for the currently logged in user.
+   * Returns all role assignments for all users.
    */
-  @Get('/role-assignments')
+  @Get('/assignments')
   @UseGuards(AuthGuard)
-  @ApiOperation({ summary: "Get the current user's roles." })
+  @ApiOperation({ summary: "Get all role assignments." })
   @ApiSecurity(ApiSecurityTypes.LOCAL_USER_JWT)
-  async getRoleAssignments(@CurrentUser() user): Promise<RoleAssignment[]> {
-    const assignments = await this.rbacService.getRoleAssignments(user)
+  async getRoleAssignments(): Promise<RoleAssignment[]> {
+    const assignments = await this.rbacService.getAndCountRoleAssignments()
     return assignments
   }
 
@@ -47,16 +51,25 @@ export class RBACController {
    * Assigns roles by creating a role assignments. If the user already has the
    * role, the existing assignment will silently returned instead of creating a
    * new one.
+   * 
+   * TODO check for Role.Assign capability
    */
-  @Post('/role-assignments')
+  @Post('/:role/assignments')
   @UseGuards(AuthGuard)
   @ApiOperation({ summary: 'Assign one or more roles.' })
   @ApiSecurity(ApiSecurityTypes.LOCAL_USER_JWT)
   async assignRoles(
-    @Body() { roles }: AssignRolesDto,
-    @CurrentUser() user: User,
+    @Body() { userIds }: AssignRolesDto,
+    @Param('role') role: RoleNames,
   ): Promise<RoleAssignment[]> {
-    const assignments = await this.rbacService.assignRoles(user, roles)
+    const userEntities: User[] = []
+
+    for (const userId of userIds) {
+      const entity = await this.userService.getUserByLocalId(userId)
+      userEntities.push(entity)
+    }
+
+    const assignments = await this.rbacService.assignRole(role, userEntities)
 
     if (!Array.isArray(assignments)) {
       throw new InternalServerErrorException()
@@ -67,16 +80,25 @@ export class RBACController {
 
   /**
    * Revokes roles by deleting the assignment.
+   * 
+   * TODO check for Role.Assign capability
    */
-  @Delete('/role-assignments/:role')
+  @Delete('/:role/assignments')
   @ApiOperation({ summary: 'Revoke roles.' })
   @UseGuards(AuthGuard)
   @ApiSecurity(ApiSecurityTypes.LOCAL_USER_JWT)
   async revokeRoles(
-    @Param() { roles }: RevokeRolesDto,
-    @CurrentUser() user: User,
+    @Query() { userIds }: RevokeRolesDto,
+    @Param('role') role: RoleNames,
   ): Promise<RoleAssignment[]> {
-    const deleted = await this.rbacService.revokeRoles(user, roles)
+    const userEntities: User[] = []
+
+    for (const userId of userIds) {
+      const entity = await this.userService.getUserByLocalId(userId)
+      userEntities.push(entity)
+    }
+
+    const deleted = await this.rbacService.revokeRole(role, userEntities)
     return deleted
   }
 }
