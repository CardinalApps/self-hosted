diff a/servers/media/src/modules/rbac/rbac.controller.ts b/servers/media/src/modules/rbac/rbac.controller.ts	(rejected hunks)
@@ -15,7 +15,7 @@ import {
   ApiSecurity,
   ApiOperation,
 } from '@nestjs/swagger'
-import { RoleNames } from '@cardinalapps/access-control/dist/cjs'
+import { getRole, RoleNames } from '@cardinalapps/access-control/dist/cjs'
 
 import { AuthGuard } from '../../guards/auth.guard'
 import { ApiSecurityTypes } from '../../guards/types'
@@ -79,6 +79,10 @@ export class RBACController {
       userEntities.push(entity)
     }
 
+    if (!getRole(role).assignable) {
+      throw new Error(`The role "${role}" is not assignable.`)
+    }
+
     const assignments = await this.rbacService.assignRole(role, userEntities)
 
     if (!Array.isArray(assignments)) {
