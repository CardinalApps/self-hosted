diff a/servers/media/src/modules/app/app.controller.ts b/servers/media/src/modules/app/app.controller.ts	(rejected hunks)
@@ -10,16 +10,12 @@ import {
   Logger,
   InternalServerErrorException,
   ServiceUnavailableException,
-  UseGuards,
   BadRequestException,
 } from '@nestjs/common'
 import {
   ApiTags,
-  ApiOperation,
   ApiOkResponse,
-  ApiSecurity,
   ApiCreatedResponse,
-  ApiForbiddenResponse,
   ApiServiceUnavailableResponse,
 } from '@nestjs/swagger'
 import { HttpAdapterHost } from '@nestjs/core'
@@ -28,14 +24,12 @@ import { AllSettingsSlugs, getSetting } from '@cardinalapps/app-settings/dist/cj
 import { AppService } from './app.service'
 import { DatabaseService } from '../database/database.service'
 import { SettingsService } from '../settings/settings.service'
+import { StandardEndpoint } from '../../decorators/StandardEndpoint.decorator'
 
 import { ServerSetupDto } from './dtos/ServerSetup.dto'
 import { ServerResetDto } from './dtos/ServerReset.dto'
 import { ListDirectoryDto } from './dtos/ListDirectory.dto'
 
-import { AuthGuard } from '../../guards/auth.guard'
-import { ApiSecurityTypes } from '../../guards/types'
-
 import { CardinalApp } from '../../utils/apps'
 import { helpCode } from '../../utils/help-codes'
 import { MediaType } from '../../utils/media'
@@ -54,6 +48,7 @@ import {
 import { envVar, getMediaDirs } from '../../utils/env'
 
 @Controller()
+@ApiTags('General')
 export class AppController {
   constructor(
     private readonly appService: AppService,
@@ -68,8 +63,10 @@ export class AppController {
    */
   @Get('/')
   @Version([VERSION_NEUTRAL])
-  @ApiTags('General')
-  @ApiOperation({ summary: 'Get information about your API.' })
+  @StandardEndpoint({
+    auth: false,
+    summary: 'Get information about your API.',
+  })
   @ApiOkResponse({
     status: 200,
     description: 'Lists all available versions of your API, and all endpoints across all versions of your API.',
@@ -103,8 +100,10 @@ export class AppController {
    * 1. Trigger an update check (infrequently).
    */
   @Get('/health')
-  @ApiTags('General')
-  @ApiOperation({ summary: 'Know if your Media Server is online.' })
+  @StandardEndpoint({
+    auth: false,
+    summary: 'Know if your Media Server is online.',
+  })
   @ApiOkResponse({
     status: 200,
     description: `Returns the state of the server. If the server responds, then it's online.
@@ -146,8 +145,10 @@ export class AppController {
    * Get the current release channel.
    */
   @Get('/release-channels')
-  @ApiTags('General')
-  @ApiOperation({ summary: 'Get the current release channel.' })
+  @StandardEndpoint({
+    auth: false,
+    summary: 'Get the current release channel.',
+  })
   async getCurrentReleaseChannel(): Promise<HomeServerReleaseChannels> {
     return {
       current: envVar('RELEASE_CHANNEL', null),
@@ -159,8 +160,10 @@ export class AppController {
    * information about them. Does not support the development environment.
    */
   @Get('/updates')
-  @ApiTags('General')
-  @ApiOperation({ summary: 'Manually check for available updates.' })
+  @StandardEndpoint({
+    auth: false,
+    summary: 'Manually check for available updates.',
+  })
   @ApiOkResponse({
     status: 200,
     schema: {
@@ -192,10 +195,14 @@ export class AppController {
    * After the initial setup is complete, this endpoint cannot be used again.
    */
   @Post('/setup')
-  @ApiTags('General')
-  @ApiOperation({ summary: 'Initial setup.' })
+  @StandardEndpoint({
+    auth: false,
+    summary: 'Initial setup.',
+    errors: {
+      403: ['The initial setup has already been completed'],
+    },
+  })
   @ApiCreatedResponse({ description: 'Returns an empty Created response on success.' })
-  @ApiForbiddenResponse({ description: 'Returns a Forbidden error if initial setup has already been completed.' })
   async setup(@Body() serverSetupDto: ServerSetupDto): Promise<ServerInitResultType | false> {
     if (await this.appService.isServerSetup()) {
       Logger.error(`Forbidden - First time setup may only be completed once. ${helpCode('0010')}` )
@@ -227,8 +234,9 @@ export class AppController {
    * the timestamp is of the build, not the commit.
    */
   @Get('/versions')
-  @ApiTags('General')
-  @ApiOperation({ summary: 'Get versions of things.' })
+  @StandardEndpoint({
+    summary: 'Get versions of things.',
+  })
   @ApiOkResponse({
     status: 200,
     schema: {
@@ -245,8 +253,6 @@ export class AppController {
       } as Versions,
     },
   })
-  @UseGuards(AuthGuard)
-  @ApiSecurity(ApiSecurityTypes.LOCAL_USER_JWT)
   async getAppVersions(): Promise<Versions> {
     return {
       nodejs: process.versions.node,
@@ -265,8 +271,9 @@ export class AppController {
    * Handles resetting things.
    */
   @Post('/reset')
-  @ApiTags('General')
-  @ApiOperation({ summary: 'Reset server state.' })
+  @StandardEndpoint({
+    summary: 'Reset server state.',
+  })
   async reset(@Body() { type, validationString }: ServerResetDto): Promise<boolean> {
     if (type === ResetType.MEDIA && validationString !== ResetValidationPhrase.MEDIA) {
       throw new Error('You must enter the validation phrase.')
@@ -288,14 +295,15 @@ export class AppController {
 
   /**
    * Lists the contents of a directory. Only some directories that are owned by
-   * Cardial Media Server can be browsed.
+   * Cardinal Media Server can be browsed.
    */
   @Get('/ls')
-  @ApiTags('General')
-  @ApiOperation({ summary: 'List the contents of a directory.' })
-  @ApiOkResponse({ status: 200 })
-  @UseGuards(AuthGuard)
-  @ApiSecurity(ApiSecurityTypes.LOCAL_USER_JWT)
+  @StandardEndpoint({
+    summary: 'List the contents of a directory.',
+    errors: {
+      400: ['Missing root directory'],
+    },
+  })
   async ls(@Query() { rootDir, path, removeFileNodes, addEmptyNodes }: ListDirectoryDto): Promise<Record<string, unknown>> {
     const mounted = getMediaDirs()
     let rootPath
