export enum HealthStates {
  NORMAL = 'normal',
  NOT_SETUP = 'not_setup',
}

export enum ResetType {
  MEDIA = 'media',
  FACTORY = 'factory',
}

export enum ResetValidationPhrase {
  MEDIA = 'Deindex media',
  FACTORY = 'Factory reset',
}
