diff a/servers/media/src/modules/app/app.module.ts b/servers/media/src/modules/app/app.module.ts	(rejected hunks)
@@ -81,8 +81,7 @@ const resolvePostgresHost = () => {
 
 @Module({
   imports: [
-    // Initialize Nest modules
-    // @ts-expect-error FIXME separate the db configs and set their types properly
+    // @ts-expect-error FIXME didn't feel like typing this when converting from vanilla JS
     TypeOrmModule.forRoot(envVar('CARDINAL_POSTGRES', false)
       ? {
           type: 'postgres',
@@ -92,6 +91,7 @@ const resolvePostgresHost = () => {
           password: envVar('POSTGRES_PASSWORD', 'cardinal'),
           autoLoadEntities: true,
           retryAttempts: 3,
+          // this stays on until 1.0.0
           synchronize: true,
           namingStrategy: new SnakeNamingStrategy(),
         }
@@ -101,6 +101,7 @@ const resolvePostgresHost = () => {
           database: getSQLiteDatabaseLocation(),
           autoLoadEntities: true,
           retryAttempts: 3,
+          // this stays on until 1.0.0
           synchronize: true,
         },
     ),
