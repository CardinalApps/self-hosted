diff a/servers/media/src/modules/music-release/music-release.controller.ts b/servers/media/src/modules/music-release/music-release.controller.ts	(rejected hunks)
@@ -21,7 +21,7 @@ import { ApiSecurityTypes } from '../../guards/types'
 import { MusicRelease } from './music-release.entity'
 import { MusicReleaseService } from './music-release.service'
 
-import { GetMusicArtistDto } from './dtos/GetMusicRelease.dto'
+import { GetMusicReleaseDto } from './dtos/GetMusicRelease.dto'
 import { GetMusicReleasesDto } from './dtos/GetMusicReleases.dto'
 
 import { EventService } from '../event/event.service'
@@ -45,17 +45,14 @@ export class MusicReleaseController {
   @UseGuards(AuthGuard)
   @ApiSecurity(ApiSecurityTypes.LOCAL_USER_JWT)
   async getMusicArtist(
-    @Param() {
-      id,
-      artists = false,
-      genres = false,
-      tracks = false,
-    }: GetMusicArtistDto,
+    @Param() { id }: { id: string },
+    @Query() { artists, tracks, genres, thumbnails }: GetMusicReleaseDto,
   ): Promise<MusicRelease> {
     const musicRelease = await this.musicReleaseService.get(id, {
       artists,
       genres,
       tracks,
+      thumbnails,
     })
 
     if (!musicRelease) {
