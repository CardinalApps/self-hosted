diff a/servers/media/src/modules/claim/claim.service.ts b/servers/media/src/modules/claim/claim.service.ts	(rejected hunks)
@@ -25,13 +25,6 @@ export class ClaimService {
     this.eventService.subscribePrivate(this, UserEvents.CREATE_OWNER, this.claimServerWithCloudIfNotClaimed.bind(this))
   }
 
-  /**
-   * When Nest starts up.
-   */
-  async onModuleInit(): Promise<void> {
-    this.claimServerWithCloudIfNotClaimed()
-  }
-
   /**
    * Propagate the local claim to Cardinal's cloud. This will allow the user to
    * see the server in their account portal.
@@ -67,7 +60,7 @@ export class ClaimService {
         instanceId: instanceId as string,
         origin: instanceId as string,
       },
-      JWT: owner.ssoToken,
+      JWT: options.jwt,
     })
       .then((res) => {
         this.saveClaim(res)
