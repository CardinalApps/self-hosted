diff a/servers/media/src/modules/job/job-queue.service.ts b/servers/media/src/modules/job/job-queue.service.ts	(rejected hunks)
@@ -45,7 +45,8 @@ export class JobQueueService implements QueueService {
    * When Nest starts up.
    */
   async onModuleInit(): Promise<void> {
-    setTimeout(() => this.resumeRunningJobs(), ms('3 seconds'))
+    // Disabling this until it can be configured by the user
+    //setTimeout(() => this.resumeRunningJobs(), ms('3 seconds'))
   }
 
   /**
