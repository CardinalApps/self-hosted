diff a/servers/media/src/modules/user/user.service.ts b/servers/media/src/modules/user/user.service.ts	(rejected hunks)
@@ -52,10 +52,6 @@ export class UserService {
    * will be validated.
    */
   async createUser(createUserDto: CreateUser): Promise<User | null> {
-    if (createUserDto?.cardinalJWT && await this.getServerOwner()) {
-      throw new Error('Cannot create more than 1 online local account')
-    }
-
     let cardinalCloudUser
 
     // Fetch the user from the auth servers for this JWT and validate it
@@ -311,4 +323,34 @@ export class UserService {
       cachedCloudUserAt: userObject.cachedCloudUserAt,
     }
   }
+
+
+  /**
+   * TODO move seats to a new service
+   */
+
+
+  /**
+   * Returns an array of all users that are using a seat.
+   */
+  async getSeatedUsers(): Promise<User[]> {
+    const seatedUsers = await this.userRepository.find({
+      where: {
+        cardinalId: Not(IsNull()),
+      },
+    })
+    return seatedUsers
+  }
+
+  /**
+   * Returns the number of users that are using seats.
+   */
+  async countSeatedUsers(): Promise<number> {
+    const seatedUsers = await this.userRepository.count({
+      where: {
+        cardinalId: Not(IsNull()),
+      },
+    })
+    return seatedUsers
+  }
 }
