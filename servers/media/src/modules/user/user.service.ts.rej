diff a/servers/media/src/modules/user/user.service.ts b/servers/media/src/modules/user/user.service.ts	(rejected hunks)
@@ -271,7 +271,15 @@ export class UserService {
    */
   async getUserByLocalJWT(localJWT: string): Promise<User | null> {
     const payload = getJWTPayload(localJWT) as LocalUserJWTPayload
-    return await this.userRepository.findOneBy({ userId: payload?.uid })
+    const a = await this.userRepository.findOne({
+      where: {
+        userId: payload?.uid,
+      },
+      relations: {
+        roles: true,
+      },
+    })
+    return a
   }
 
   /**
@@ -386,6 +394,7 @@ export class UserService {
       cardinalId: userObject.cardinalId,
       cachedCloudUser: userObject.cachedCloudUser,
       cachedCloudUserAt: userObject.cachedCloudUserAt,
+      roles: userObject.roles,
     }
   }
 }
