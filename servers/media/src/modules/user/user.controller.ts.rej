diff a/servers/media/src/modules/user/user.controller.ts b/servers/media/src/modules/user/user.controller.ts	(rejected hunks)
@@ -7,6 +7,7 @@ import {
   Post,
   UseGuards,
   MethodNotAllowedException,
+  Query,
 } from '@nestjs/common'
 import {
   ApiTags,
@@ -27,6 +28,7 @@ import { GetUserResponse } from './dtos/GetUserResponse.dto'
 import { AuthGuard } from '../../guards/auth.guard'
 import { ApiSecurityTypes } from '../../guards/types'
 import { getJWTFromHeaders } from '../../utils/jwt'
+import { GetUsersDto } from './dtos/GetUsers.dto'
 
 @Controller()
 export class UserController {
@@ -96,17 +98,13 @@ export class UserController {
   }
 
   /**
-   * Returns all users for this server.
-   * 
-   * This is a public endpoint so that it can be used on the login screen.
-   * Because of that, the objects returned here are stripped of all sensitive
-   * information. To get the full user object, send a request to `/user/{id}`.
+   * Returns all users in this server.
    */
   @Get('/users')
   @ApiTags('User')
   @ApiOperation({ summary: 'Get all users in this server.' })
-  async getUsers(): Promise<Partial<User>[]> {
-    return await this.userService.getUsersForPublic()
+  async getUsers(@Query() query: GetUsersDto): Promise<[Partial<User>[], number]> {
+    return await this.userService.query(query)
   }
 
   /**
