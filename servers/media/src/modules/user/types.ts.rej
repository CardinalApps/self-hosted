diff a/servers/media/src/modules/user/types.ts b/servers/media/src/modules/user/types.ts	(rejected hunks)
@@ -1,23 +1,3 @@
-export enum Roles {
-  /**
-   * The server owner is the top role and there can only be one per home server.
-   */
-  OWNER = 'owner',
-
-  /**
-   * The user role is the default role for local accounts if no other role is
-   * assigned during the account creation.
-   */
-  USER = 'user',
-
-  /**
-   * Invitees are people that have accepted an invitation to this server.
-   * 
-   * TODO server sharing feature and rename to "guest"
-   */
-  INVITEE = 'invitee',
-}
-
 export enum Designations {
   GUEST_ACCOUNT = 'guest_account',
 }
