diff a/servers/media/src/modules/auth/auth.controller.ts b/servers/media/src/modules/auth/auth.controller.ts	(rejected hunks)
@@ -39,7 +39,7 @@ export class AuthController {
       return await this.authService.login(loginDetails?.userId, loginDetails?.cardinalJWT)
     } catch (error) {
       Logger.error(`Login error: ${error}`, 'Auth')
-      throw new ForbiddenException()
+      throw new ForbiddenException(error.toString())
     }
   }
 }
