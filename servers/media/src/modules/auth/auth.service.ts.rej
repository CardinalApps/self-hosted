diff a/servers/media/src/modules/auth/auth.service.ts b/servers/media/src/modules/auth/auth.service.ts	(rejected hunks)
@@ -1,181 +1,46 @@
 import { Injectable, Logger } from '@nestjs/common'
-import { JwtService } from '@nestjs/jwt'
 
-import { getJWTPayload } from '../../utils/jwt'
-
-import { SettingsService } from '../settings/settings.service'
 import { UserService } from '../user/user.service'
-import { CloudUserService } from '../user/cloud-user.service'
-import { Designations, Roles } from '../user/types'
 
 import { LoginResponse } from './dtos/LoginResponse.dto'
+import { CardinalSSOStrategy } from './strategies/cardinal-sso.service'
+import { LocalAuthStrategy } from './strategies/local.service'
 
-
-// CONTINUE HERE, limit of x users (depending on tier)
-
-
-
-/**
- * This class handles auth tasks.
- */
 @Injectable()
 export class AuthService {
   constructor(
     private readonly userService: UserService,
-    private readonly cloudUserService: CloudUserService,
-    private readonly jwtService: JwtService,
-    private readonly settingsService: SettingsService,
+    private readonly cardinalSSOStrategy: CardinalSSOStrategy,
+    private readonly localAuthStrategy: LocalAuthStrategy,
   ) {}
 
   /**
-   * Creates a new JWT for a local user account.
+   * Attempts to logs into an account in this server.
    * 
-   * If a Cardinal JWT is given, then some data from it will be copied into the
-   * Media Server JWT.
-   */
-  private async createJWT(userId: string, cardinalJWT?: string): Promise<string | null> {
-    const user = await this.userService.get(userId)
-
-    if (!user) {
-      Logger.warn('Invalid user ID', 'Auth')
-      return null
-    }
-
-    const cardinalJWTPayload = cardinalJWT ? getJWTPayload(cardinalJWT) : null
-
-    return this.jwtService.sign({
-      uid: user.userId,
-      role: user.role,
-      designation: user.designation,
-      cardinalId: cardinalJWTPayload ? cardinalJWTPayload.userId : null,
-      exp: Date.now() + 315360000000, // 10 years
-    })
-  }
-
-  /**
-   * Checks whether the given JWT was signed by this server.
-   */
-  verifyJWT(JWT): boolean {
-    try {
-      const result = this.jwtService.verify(JWT)
-      return typeof result === 'object' && 'uid' in result
-    } catch (error) {
-      return false
-    }
-  }
-
-  /**
-   * Logs into a local account. If the localUserId is undefined then the ssoJWT
-   * will be used to locate the local user account.
-   * 
-   * For offline local accounts, there is no auth. For online local accounts, a
-   * Cardinal SSO JWT is required.
+   * This currently only supports Cardinal cloud accounts and a single shared
+   * Guest Account.
    */
   async login(localUserId?: string, ssoJWT?: string): Promise<LoginResponse> {
     if (!localUserId && !ssoJWT) {
       throw new Error('Either a local user ID or a SSO token is required to log in')
     }
 
-    const desiredUser = localUserId
-      ? await this.userService.get(localUserId)
-      : await this.userService.getUserByCardinalJWT(ssoJWT)
-
-    // This is the first time logging in with this cloud user account
-    if (!desiredUser && ssoJWT) {
-      const ownerAccount = await this.userService.getServerOwner()
-
-      if (ownerAccount) {
-        throw new Error('This server is already owned by another Cardinal account')
-      }
-
-      // There is no owner, claim the server
-      const newOwnerAccount = await this.userService.createUser({
-        role: Roles.OWNER,
-        cardinalJWT: ssoJWT,
-      })
-
-      if (!newOwnerAccount) {
-        throw new Error('Could not create new owner account')
-      }
-
+    if (localUserId && !ssoJWT) {
       try {
-        return await this.loginAsOnlineUser(newOwnerAccount.userId, ssoJWT)
+        return await this.localAuthStrategy.login(localUserId)
       } catch (error) {
-        Logger.error(error)
+        Logger.log(error)
+        throw error
       }
     }
 
-    if (!desiredUser) {
-      throw new Error('Invalid user ID')
-    }
-
-    // Offline user
-    if (!desiredUser?.cardinalId) {
-      return await this.loginAsOfflineUser(localUserId)
-    } else {
-      if (!ssoJWT) {
-        throw new Error('Cannot log into an online local account without a SSO JWT')
-      }
-
+    if (!localUserId && ssoJWT) {
       try {
-        return await this.loginAsOnlineUser(desiredUser.userId, ssoJWT)
+        return await this.cardinalSSOStrategy.login(localUserId, ssoJWT)
       } catch (error) {
         Logger.error(error)
+        throw error
       }
     }
   }
-
-  /**
-   * Logs into an offline local account.
-   * 
-   * FIXME test what happens if the local user ID is undefined
-   */
-  private async loginAsOfflineUser(localUserId: string): Promise<LoginResponse> {
-    const user = await this.userService.getUserByLocalId(localUserId)
-    const JWT = await this.createJWT(localUserId)
-    const guestAccountIsEnabled = await this.userService.guestAccountIsEnabled()
-
-    if (user.designation === Designations.GUEST_ACCOUNT && !guestAccountIsEnabled) {
-      throw new Error('Cannot log into the guest account because it has been disabled.')
-    }
-
-    Logger.log(`Local user ${user.userId} logged in`, 'Auth')
-
-    return {
-      JWT,
-      user: this.userService.cleanseUserObject(user),
-    }
-  }
-
-  /**
-   * Logs into an online local account.
-   */
-  private async loginAsOnlineUser(localUserId: string, cardinalSSOToken): Promise<LoginResponse> {
-    const localUser = await this.userService.get(localUserId)
-    let cloudUser
-
-    try {
-      cloudUser = await this.cloudUserService.getCardinalCloudUser(cardinalSSOToken)
-    } catch (error) {
-      throw new Error('Cannot log into online account - SSO token rejected')
-    }
-
-    if (localUser.cardinalId !== cloudUser?.userId) {
-      throw new Error('Cannot log into online account - invalid SSO token')
-    }
-
-    // Update the cached SSO token
-    try {
-      await this.userService.update(localUser.userId, { ssoToken: cardinalSSOToken })
-    } catch {
-      Logger.error('Could not update cached SSO token')
-    }
-
-    return {
-      JWT: await this.createJWT(localUserId),
-      user: this.userService.cleanseUserObject(localUser),
-      cloudJWT: localUser.ssoToken,
-      cloudUser: localUser.cachedCloudUser,
-    }
-  }
 }
