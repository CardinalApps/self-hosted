diff a/servers/media/src/modules/auth/strategies/local.service.ts b/servers/media/src/modules/auth/strategies/local.service.ts	(rejected hunks)
@@ -33,7 +33,7 @@ export class LocalAuthStrategy {
       return await this.loginWithUserId(app, localUserId)
     }
 
-    if (localUsername && localPassword) {
+    if (localUsername) {
       return await this.loginWithUsernameAndPassword(app, localUsername, localPassword)
     }
   }
