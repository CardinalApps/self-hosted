diff a/servers/media/src/modules/auth/strategies/cardinal-sso.service.ts b/servers/media/src/modules/auth/strategies/cardinal-sso.service.ts	(rejected hunks)
@@ -128,7 +128,10 @@ export class CardinalSSOStrategy {
    */
   private async handleNewCardinalUser(cardinalSSOToken): Promise<LoginResponse> {
     const newUser = await this.userService.createUser({
-      cardinalJWT: cardinalSSOToken,
+      dto: {
+        cardinalJWT: cardinalSSOToken,
+        role: 'newcomer',
+      },
     })
     return await this.loginSuccess(newUser, cardinalSSOToken)
   }
