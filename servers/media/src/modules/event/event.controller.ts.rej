diff a/servers/media/src/modules/event/event.controller.ts b/servers/media/src/modules/event/event.controller.ts	(rejected hunks)
@@ -16,8 +16,8 @@ import { Response } from 'express'
 import { v4 as uuid } from 'uuid'
 
 import { EventService } from './event.service'
-import { AuthService } from '../auth/auth.service'
 import { UserService } from '../user/user.service'
+import { TokenService } from '../auth/token.service'
 
 import { SubscribeDto } from './dtos/Subscribe.dto'
 
@@ -34,7 +34,7 @@ import { log, LogModule, LogLevel } from '../../utils/logging'
 export class EventController {
   constructor(
     private readonly eventService: EventService,
-    private readonly authService: AuthService,
+    private readonly tokenService: TokenService,
     private readonly userService: UserService,
   ) {}
 
@@ -55,7 +55,7 @@ export class EventController {
     @Query() query: SubscribeDto,
     @Res() response: Response,
   ) {
-    const tokenIsValid = this.authService.verifyJWT(query.authorization)
+    const tokenIsValid = this.tokenService.verifyJWT(query.authorization)
     if (!tokenIsValid) throw new UnauthorizedException()
 
     const user = await this.userService.getUserByLocalJWT(query.authorization)
