diff a/servers/media/src/modules/invitation/invitation.controller.ts b/servers/media/src/modules/invitation/invitation.controller.ts	(rejected hunks)
@@ -2,7 +2,6 @@ import {
   Controller,
   Get,
   Query,
-  UseGuards,
   NotFoundException,
   Param,
   Post,
@@ -12,13 +11,8 @@ import {
 } from '@nestjs/common'
 import {
   ApiTags,
-  ApiSecurity,
-  ApiOperation,
 } from '@nestjs/swagger'
 
-import { AuthGuard } from '../../guards/auth.guard'
-import { ApiSecurityTypes } from '../../guards/types'
-
 import { Invitation } from './invitation.entity'
 import { InvitationService } from './invitation.service'
 
@@ -30,8 +24,10 @@ import { EventService } from '../event/event.service'
 import { CreateInvitationDto } from './dtos/CreateInvitation'
 import { DeleteInvitationDto } from './dtos/DeleteInvitationDto'
 import { User } from '../user/user.entity'
+import { StandardEndpoint } from '../../decorators/StandardEndpoint.decorator'
 
 @Controller('/invitations')
+@ApiTags('Invitations')
 export class InvitationController {
   constructor(
     private readonly invitationService: InvitationService,
@@ -42,10 +38,10 @@ export class InvitationController {
    * Get an invitation.
    */
   @Get(':id')
-  @ApiTags('Invitations')
-  @ApiOperation({ summary: 'Get data about an invitation.' })
-  @UseGuards(AuthGuard)
-  @ApiSecurity(ApiSecurityTypes.LOCAL_USER_JWT)
+  @StandardEndpoint({
+    summary: 'Get data about an invitation.',
+    capabilities: ['Invitations.Read'],
+  })
   async getInvitation(@Param() { id }: GetInvitationDto): Promise<Invitation> {
     const invite = await this.invitationService.get(id)
 
@@ -60,10 +56,10 @@ export class InvitationController {
    * Query invitations
    */
   @Get('/')
-  @ApiTags('Invitations')
-  @ApiOperation({ summary: 'Query invitations.' })
-  @UseGuards(AuthGuard)
-  @ApiSecurity(ApiSecurityTypes.LOCAL_USER_JWT)
+  @StandardEndpoint({
+    summary: 'Query invitations.',
+    capabilities: ['Invitations.Read'],
+  })
   async queryInvitations(@Query() query: QueryInvitationDto): Promise<[Invitation[], number]> {
     return await this.invitationService.query(query)
   }
@@ -72,10 +68,10 @@ export class InvitationController {
    * Create a new invitation.
    */
   @Post('/')
-  @UseGuards(AuthGuard)
-  @ApiTags('Invitations')
-  @ApiOperation({ summary: 'Create a new invitation.' })
-  @ApiSecurity(ApiSecurityTypes.LOCAL_USER_JWT)
+  @StandardEndpoint({
+    summary: 'Create a new invitation.',
+    capabilities: ['Invitations.Create'],
+  })
   async createInvitation(
     @Body() createInvitationsDto: CreateInvitationDto,
     @CurrentUser() user: User,
@@ -88,10 +84,10 @@ export class InvitationController {
    * Delete an invitation.
    */
   @Delete(':id')
-  @UseGuards(AuthGuard)
-  @ApiTags('Invitations')
-  @ApiOperation({ summary: 'Delete a new invitation.' })
-  @ApiSecurity(ApiSecurityTypes.LOCAL_USER_JWT)
+  @StandardEndpoint({
+    summary: 'Delete a new invitation.',
+    capabilities: ['Invitations.Delete'],
+  })
   async deleteInvitation(
     @Param() { id }: DeleteInvitationDto,
     @CurrentUser() user: User,
