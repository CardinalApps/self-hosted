diff a/libraries/access-control/src/roles.ts b/libraries/access-control/src/roles.ts	(rejected hunks)
@@ -1,8 +1,11 @@
+import { CapabilitityString } from "./capabilities"
+
 export type Role = {
   assignable: boolean,
   unassignable: boolean,
   maxUsers: number | null, // Use null for unlimited
   slug: string,
+  capabilities: CapabilitityString[],
 }
 
 export const Roles: Record<string, Role> = {
@@ -11,12 +14,20 @@ export const Roles: Record<string, Role> = {
     unassignable: false,
     maxUsers: 1,
     slug: 'owner',
+    capabilities: [
+      'AdminApp.Login',
+    ],
   },
   newcomer: {
     assignable: true,
     unassignable: true,
     maxUsers: null,
     slug: 'newcomer',
+    capabilities: [
+      'MusicApp.Login',
+      'PhotosApp.Login',
+      'CinemaApp.Login',
+    ],
   },
 } as const
 
