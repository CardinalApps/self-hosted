diff a/libraries/access-control/src/capabilities.ts b/libraries/access-control/src/capabilities.ts	(rejected hunks)
@@ -1,32 +1,33 @@
 /**
- * A capability is a single string that combines an aspect and an action,
- * separating them with a dot.
+ * A capability is a string that combines an aspect and an action, separating
+ * them with a dot, like so:
  * 
- *    {Aspect}.{Action}
+ *    - `{Aspect}.{Action}`
  * 
- * Capability Assignments are stored as an array of capability strings. The
- * strings can be exact matching capabilities, or can use of a wildcard for
- * selecting broader ranges of capabilities.
+ * Capability *Assignments* are either literal capabilities like above, or can
+ * include wildcards for selecting broader ranges of capabilities.
  * 
- *    `*.*` - Matches all aspects and all actions, granting all current and
- *    future capabilities.
+ *    - `*.*` - Matches all aspects and all actions, granting all current and
+ *      future capabilities.
  * 
- *    `MusicApp.*` - Matches all actions in the MusicApp aspect, granting all
- *    current and future MusicApp capabilities.
+ *    - `MusicApp.*` - Matches all actions in the MusicApp aspect, granting all
+ *      current and future MusicApp capabilities.
  * 
- *    `*.Login` - Matches only the Login action on all current and future
- *    aspects.
+ *    - `*.Login` - Matches only the Login action on all current and future
+ *      aspects.
  * 
- *    `CinemaApp.Login` - Matches only the Login action on the CinemaApp
- *    aspect.
+ *    - `CinemaApp.Login` - Matches only the Login action on the CinemaApp
+ *      aspect.
+ * 
+ * Use the generic to scope the CapabilityAssignment to a specific RBAC context.
  */
-export type CapabilityAssignment =
+export type CapabilityAssignment<T> =
+  T
   | '*.*'
   | `${string}.*`
   | `*.${string}`
-  | `${string}.${string}`
 
-export type CapabilityAssignments = CapabilityAssignment[]
+export type CapabilityAssignments<T> = CapabilityAssignment<T>[]
 
 /**
  * Validate the syntax of an capability assignment. This will check:
@@ -37,7 +38,7 @@ export type CapabilityAssignments = CapabilityAssignment[]
  * 
  * This does *not* check that the capability exists in the master list.
  */
-const validateCapabilitySyntax = (assignment: string): boolean => {
+const validateCapabilitySyntax = <TCapabilityContext>(assignment: CapabilityAssignment<TCapabilityContext>): boolean => {
   if (typeof assignment !== 'string') {
     return false
   }
@@ -66,7 +67,7 @@ const validateCapabilitySyntax = (assignment: string): boolean => {
  * Validate the syntax of an array of capability assignments. All array items
  * must be valid.
  */
-const validateCapabilitiesSyntax = (capabilityList: CapabilityAssignments = []): boolean => {
+const validateCapabilitiesSyntax = <TCapabilityContext>(capabilityList: CapabilityAssignments<TCapabilityContext> = []): boolean => {
   let allAreValid = true
 
   for (const capability of capabilityList) {
@@ -84,8 +85,7 @@ const validateCapabilitiesSyntax = (capabilityList: CapabilityAssignments = []):
  */
 export function hasCapability<TCapabilityContext extends string>(
   requiredCapability: TCapabilityContext,
-  // FIXME do I need this type? It has all caps from all contexts
-  assignments: CapabilityAssignments = [],
+  assignments: CapabilityAssignments<TCapabilityContext> = [],
 ) {
   if (!validateCapabilitySyntax(requiredCapability)) {
     return false
@@ -134,7 +134,7 @@ export function hasCapability<TCapabilityContext extends string>(
  */
 export function hasCapabilities<TCapabilityContext extends string>(
   requiredCapabilities: TCapabilityContext[],
-  assignments: CapabilityAssignments = [],
+  assignments: CapabilityAssignments<TCapabilityContext> = [],
 ) {
   if (!requiredCapabilities) {
     return true
@@ -145,7 +145,7 @@ export function hasCapabilities<TCapabilityContext extends string>(
   }
 
   const allCapabilitiesAreSatisfied = requiredCapabilities.every((requiredCapability) => {
-    const satisfiedByRole = assignments?.find((assignment: CapabilityAssignment) => {
+    const satisfiedByRole = assignments?.find((assignment: CapabilityAssignment<TCapabilityContext>) => {
       return !!hasCapability<TCapabilityContext>(requiredCapability, [assignment])
     })
     return !!satisfiedByRole
