diff a/libraries/access-control/src/capabilities.ts b/libraries/access-control/src/capabilities.ts	(rejected hunks)
@@ -6,15 +6,18 @@
  * Resources, Features, Applications, and Services are all categorized as
  * components within the Access Control layer.
  */
-export type Components =
-    'Users'
-  | 'AdminApp'
-  | 'MusicApp'
-  | 'MusicTracks'
-  | 'MusicReleases'
-  | 'MusicArtists'
-  | 'PhotosApp'
-  | 'CinemaApp'
+export const Components = [
+  'AdminApp',
+  'MusicApp',
+  'MusicTracks',
+  'MusicReleases',
+  'MusicArtists',
+  'PhotosApp',
+  'CinemaApp',
+  'Users',
+] as const
+
+export type ComponentNames = typeof Components[number]
 
 /**
  * Master list of all capabilities.
@@ -74,18 +77,21 @@ export type CapabilityString = typeof Capabilities[number]
  * selecting broader ranges of capabilities.
  * 
  *    `*.*` - Matches all components and all actions, granting all current and
- *    future capabilities. Reserved for Owner account.
+ *    future capabilities.
  * 
  *    `MusicApp.*` - Matches all actions in the MusicApp component, granting all
  *    current and future MusicApp capabilities.
  * 
- *    `*.Login` - Matches only Login actions on all current and future
+ *    `*.Login` - Matches only the Login action on all current and future
  *    components.
+ * 
+ *    `CinemaApp.Login` - Matches only the Login action on the CinemaApp
+ *    component.
  */
 export type CapabilityAssignment =
   CapabilityString
   | '*.*'
-  | `${Components}.*`
+  | `${ComponentNames}.*`
   | `*.${string}`
 
 export type CapabilityAssignments = CapabilityAssignment[]
@@ -121,7 +127,7 @@ const validateCapabilitySyntax = (assignment: CapabilityAssignment): boolean =>
 /**
  * Validate the syntax of an array of capability assignments.
  */
-const validateCapabilitiesSyntax = (capabilityList: CapabilityAssignments): boolean => {
+const validateCapabilitiesSyntax = (capabilityList: CapabilityAssignments = []): boolean => {
   let allAreValid = true
 
   for (const capability of capabilityList) {
@@ -138,7 +144,7 @@ const validateCapabilitiesSyntax = (capabilityList: CapabilityAssignments): bool
  */
 export function hasCapability(
   requiredCapability: CapabilityString,
-  assignments: CapabilityAssignments,
+  assignments: CapabilityAssignments = [],
 ) {
   if (!validateCapabilitySyntax(requiredCapability)) {
     return false
@@ -148,6 +154,10 @@ export function hasCapability(
     return false
   }
 
+  if (!assignments.length) {
+    return false
+  }
+
   const [requiredComponent, requiredAction] = requiredCapability.split('.')
 
   for (const assignment of assignments) {
