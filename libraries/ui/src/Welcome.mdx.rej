diff a/libraries/ui/src/Welcome.mdx b/libraries/ui/src/Welcome.mdx	(rejected hunks)
@@ -20,7 +20,7 @@ Storybook is configured with Redux to enable all app features.
 
 ## Types of Components
 
-All React components fall into one of four categories:
+All React components fall into one of five categories:
 
 ### Feature Components
 
@@ -28,6 +28,11 @@ Feature components implement complex features and are designed to plug-and-play
 with all Cardinal apps. They combine multiple other components, RTK slices, RTK
 APIs, handle logic and state, and perform network requests.
 
+### Form Components
+
+Form components are used as fields in forms. They can also be wrapped in the
+`<form>` component.
+
 ### Interaction Components
 
 Interaction components are for the parts of the UI that the user interacts with.
@@ -37,7 +42,7 @@ components.
 
 ### Layout Components
 
-Layout components exist solely for presentational purposes, and are used to
+Layout components exist solely for page purposes, and are used to
 scaffold the interface. They don't handle events or logic.
 
 ### Typography Components
