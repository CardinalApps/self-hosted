diff a/libraries/ui/src/components/features/AppBase/layouts/Procedural.tsx b/libraries/ui/src/components/features/AppBase/layouts/Procedural.tsx	(rejected hunks)
@@ -13,6 +13,7 @@ import H5 from '../../../typography/H5'
 export type ProceduralProps = {
   name: string,
   className?: string | string[],
+  isReady?: boolean,
   hasContent?: boolean,
   emptyTitle?: boolean,
   emptyMessage?: boolean,
@@ -26,6 +27,7 @@ export type ProceduralProps = {
 function ProceduralLayout({
   className,
   children,
+  isReady = false,
   hasContent = false,
   emptyTitle,
   emptyMessage,
@@ -41,7 +43,7 @@ function ProceduralLayout({
     }
   }, [atBottom])
 
-  if (!hasContent) {
+  if (isReady && !hasContent) {
     return (
       <div className="procedural-empty">
         <Card
@@ -59,7 +61,7 @@ function ProceduralLayout({
     )
   }
 
-  return (
+  return hasContent && isReady && (
     <div
       ref={layoutRef}
       className={clsx('procedural-layout', className)}
