diff a/libraries/ui/src/components/forms/DatePicker/DatePicker.tsx b/libraries/ui/src/components/forms/DatePicker/DatePicker.tsx	(rejected hunks)
@@ -48,7 +48,6 @@ const DatePicker = ({
     } else if (type === 'datetime-local') {
       const d = new Date(value)
       const yyyymmddhhss = `${d.getFullYear()}-${(d.getMonth()+1).toString().padStart(2, '0')}-${d.getDate().toString().padStart(2, '0')}T${d.getHours().toString().padStart(2, '0')}:${d.getMinutes().toString().padStart(2, '0')}`
-      console.log(yyyymmddhhss)
       return yyyymmddhhss
     }
   }
