diff a/libraries/ui/src/components/forms/Form/Form.tsx b/libraries/ui/src/components/forms/Form/Form.tsx	(rejected hunks)
@@ -36,10 +36,14 @@ const Form = ({
    * will have their contents parsed as JSON.
    */
   const getFormData = (e) => {
+    const formEl = e.target.matches('form')
+      ? e.target
+      : e.target.closest('form')
+
     const entries = {}
-    Array.from(new FormData(e.target).entries()).forEach(([key, value]) => entries[key] = value)
+    Array.from(new FormData(formEl).entries()).forEach(([key, value]) => entries[key] = value)
 
-    const jsonFields = Array.from(e.target.querySelectorAll('[data-is-json="true"]')).map((el: HTMLElement) => el.getAttribute('name'))
+    const jsonFields = Array.from(formEl.querySelectorAll('[data-is-json="true"]')).map((el: HTMLElement) => el.getAttribute('name'))
 
     for (const [name, value] of Object.entries(entries) ) {
       if (typeof name === 'string' && jsonFields.includes(name)) {
@@ -54,6 +58,11 @@ const Form = ({
     <form
       className={`form ${className} ${error ? 'error' : ''}`}
       style={style}
+      onKeyDown={(e) => {
+        if (e.key === 'Enter') {
+          onSubmit?.(e, getFormData(e))
+        }
+      }}
       onSubmit={(e) => {
         e.preventDefault()
         onSubmit?.(e, getFormData(e))
