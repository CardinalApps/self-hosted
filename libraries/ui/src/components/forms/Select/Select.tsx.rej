diff a/libraries/ui/src/components/forms/Select/Select.tsx b/libraries/ui/src/components/forms/Select/Select.tsx	(rejected hunks)
@@ -206,9 +206,9 @@ const Select = ({
    * Blur event for options.
    */
   const onSelectBlur = (e) => {
-    const isOutside = !e.relatedTarget?.closest('.custom-select')
+    const isOutside = !e.relatedTarget?.closest(`.custom-select[data-name=${name}]`)
 
-    if (isOutside && !e?.relatedTarget?.closest('.custom-select')) {
+    if (isOutside && !e?.relatedTarget?.closest(`.custom-select[data-name=${name}]`)) {
       setPopoutOpen(false)
     }
   }
