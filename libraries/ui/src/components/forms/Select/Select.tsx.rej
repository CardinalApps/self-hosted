diff a/libraries/ui/src/components/forms/Select/Select.tsx b/libraries/ui/src/components/forms/Select/Select.tsx	(rejected hunks)
@@ -168,7 +168,11 @@ const Select = ({
       // Only 1 is selected
       if (selected.length === 1) {
         const firstSelected = optionsAsArray(options).find((opt) => opt.value === selected[0])
-        return `${selectedPrefix}${firstSelected?.sentenceCase || firstSelected?.label}`
+        if (firstSelected) {
+          return `${selectedPrefix}${firstSelected?.sentenceCase || firstSelected?.label}`
+        } else {
+          return `${selectedPrefix}`
+        }
       }
       // Multiple are selected
       else {
