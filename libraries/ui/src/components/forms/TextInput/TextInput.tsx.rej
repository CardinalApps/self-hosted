diff a/libraries/ui/src/components/forms/TextInput/TextInput.tsx b/libraries/ui/src/components/forms/TextInput/TextInput.tsx	(rejected hunks)
@@ -5,12 +5,14 @@ import clsx from 'clsx'
 
 type TextInputProps = {
   name?: string,
+  id?: string,
   type?: string,
   value?: string | number,
   className?: string,
   placeholder?: string,
   maxLength?: number,
   disabled?: boolean,
+  defaultValue?: string,
   size?: 'm' | 'l',
   onFocus?: (value) => void,
   onBlur?: (value) => void,
@@ -23,12 +25,14 @@ type TextInputProps = {
  */
 const TextInput = ({
   name,
+  id,
   type = 'text',
   value = undefined,
   className,
   placeholder = '',
   maxLength,
   disabled,
+  defaultValue,
   size = 'm',
   onFocus = () => {},
   onBlur = () => {},
@@ -46,8 +50,10 @@ const TextInput = ({
       <input
         type={type}
         name={name}
+        id={id}
         value={value}
         disabled={disabled}
+        defaultValue={defaultValue}
         placeholder={placeholder}
         maxLength={maxLength}
         onFocus={(e) => onFocus(e.target.value)}
