diff a/libraries/ui/src/components/forms/AddRemove/AddRemove.tsx b/libraries/ui/src/components/forms/AddRemove/AddRemove.tsx	(rejected hunks)
@@ -17,6 +17,8 @@ import './AddRemove.css'
 type Item = {
   name: string,
   value: string,
+  canAdd?: boolean,
+  canDelete?: boolean,
 }
 
 type AddRemoveProps = {
@@ -126,7 +128,7 @@ const AddRemove = ({
     if (isPendingAdd) {
       controls.push('remove')
     }
-    if (isAvailable && !isPendingAdd) {
+    if (item?.canAdd !== false && isAvailable && !isPendingAdd) {
       controls.push('add')
     }
 
