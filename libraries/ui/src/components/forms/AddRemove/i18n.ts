import { StringList } from "../../../types/i18n"

const i18n: StringList = {
  "add-remove.custom.error.duplicate.title": {
    "en": "Duplicate item",
  },
  "add-remove.custom.error.duplicate.message": {
    "en": "Each path in a library must be unique.",
  },
}

export default i18n
