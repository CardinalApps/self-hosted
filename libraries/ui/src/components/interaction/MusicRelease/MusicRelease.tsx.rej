diff a/libraries/ui/src/components/interaction/MusicRelease/MusicRelease.tsx b/libraries/ui/src/components/interaction/MusicRelease/MusicRelease.tsx	(rejected hunks)
@@ -13,7 +13,6 @@ import './MusicRelease.css'
 type MusicReleaseProps = {
   artwork: string,
   tracks?: { id: number }[],
-  musicTrackToPlayId?: number,
   releaseTitle?: string,
   releaseYear?: string,
   releaseLink?: string,
@@ -60,7 +59,7 @@ const MusicRelease = ({
         <div className="row">
           {!!releaseTitle && (
             <p className="release-name">
-              {releaseLink
+              {releaseLink && releaseTitle && Link
                 ? <Link to={releaseLink}>{releaseTitle}</Link>
                 : releaseTitle
               }
@@ -70,7 +69,7 @@ const MusicRelease = ({
         <div className="row">
           {!!artistName && (
             <p className="artist-name">
-              {artistLink
+              {artistLink && artistName && Link
                 ? <Link to={artistLink}>{artistName}</Link>
                 : artistName
               }
