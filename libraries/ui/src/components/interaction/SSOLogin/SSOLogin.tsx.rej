diff a/libraries/ui/src/components/interaction/SSOLogin/SSOLogin.tsx b/libraries/ui/src/components/interaction/SSOLogin/SSOLogin.tsx	(rejected hunks)
@@ -33,6 +33,7 @@ type TokenType = 'JWT' | 'cookie'
 export type IPCHandshakeMessageData = {
   appId: string,
   nonce: string,
+  instanceId?: string,
   serverName: string,
   userAgentString: string,
   tokenType: TokenType,
@@ -41,6 +42,7 @@ export type IPCHandshakeMessageData = {
 
 type SSOLoginProps = {
   appId: string,
+  instanceId?: string,
   /**
    * @deprecated USER-22
    */
@@ -62,6 +64,7 @@ type SSOLoginProps = {
  */
 const SSOLogin = ({
   appId,
+  instanceId,
   permissions,
   saveJWTInLocalStorage = true,
   onPopupOpened,
@@ -127,6 +130,7 @@ const SSOLogin = ({
       const dataToSend: IPCHandshakeMessageData = {
         appId,
         nonce,
+        instanceId,
         serverName: server_name as string,
         userAgentString: navigator.userAgent,
         tokenType,
@@ -210,6 +214,7 @@ const SSOLogin = ({
         body: {
           appId,
           nonce,
+          instanceId,
           tokenType,
           serverName: server_name,
         },
