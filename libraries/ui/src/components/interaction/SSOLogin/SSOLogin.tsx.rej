diff a/libraries/ui/src/components/interaction/SSOLogin/SSOLogin.tsx b/libraries/ui/src/components/interaction/SSOLogin/SSOLogin.tsx	(rejected hunks)
@@ -3,8 +3,6 @@ import type { CSSProperties, PropsWithChildren } from 'react'
 import { v4 as uuid } from 'uuid'
 
 import BrandLogo from '../../layout/BrandLogo'
-import { useAppSelector } from '../../../hooks/useAppSelector'
-import { settingsSelectors } from '../../../store/slices/settings'
 
 import { setJwt, JWT_TYPE } from '../../../lib/auth/jwt'
 import { ACCOUNT_APP_HOST } from '../../../../env'
@@ -24,6 +22,7 @@ type SSOLoginProps = {
   label?: string,
   ssoUrl?: string,
   debug?: boolean,
+  enableNewSSOFlow?: boolean,
   style?: CSSProperties,
 }
 
@@ -43,14 +42,13 @@ const SSOLogin = ({
   label = 'Sign in with Cardinal',
   ssoUrl,
   debug = false,
+  enableNewSSOFlow = false,
   style,
 }: PropsWithChildren<SSOLoginProps>) => {
   const ssoFlowWindow = useRef(null)
   const ssoWindowClosedInterval = useRef(null)
   const [ssoFlowIsOpen, setSSOFlowIsOpen] = useState(false)
 
-  const { enable_oidc_beta } = useAppSelector(settingsSelectors.current)
-
   if (!ssoUrl) {
     ssoUrl = window.location.href.includes('//localhost:') || window.location.href.includes('//127.0.0.1:')
       ? SSO_URL_DEV
@@ -125,7 +123,7 @@ const SSOLogin = ({
 
   const getPopupUrl = () => {
     // Legacy URL
-    if (!enable_oidc_beta) {
+    if (!enableNewSSOFlow) {
       return `${ssoUrl}/login?sso`
     }
 
@@ -158,7 +156,7 @@ const SSOLogin = ({
             const popupLeft = (window.outerWidth / 2) - (popupWidth / 2)
             const popupright = (window.outerHeight / 2) - (popupHeight / 2)
 
-            ssoFlowWindow.current = enable_oidc_beta
+            ssoFlowWindow.current = enableNewSSOFlow
               ? window.open(getPopupUrl())
               : window.open(getPopupUrl(), '_blank', `popup=true, width=${popupWidth}, height=${popupHeight}, left=${popupLeft}, top=${popupright}`)
 
