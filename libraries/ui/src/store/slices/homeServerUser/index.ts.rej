diff a/libraries/ui/src/store/slices/homeServerUser/index.ts b/libraries/ui/src/store/slices/homeServerUser/index.ts	(rejected hunks)
@@ -124,6 +124,7 @@ const homeServerUserSlice = createSlice({
       }
     }),
   selectors: {
+    loading: (state) => state.loading,
     loggedIn: (state) => !!Object.keys(state.current).length,
     loggedInAt: (state) => state.loggedInAt,
     loginRedirectCompletedFor: (state) => state.loginRedirectCompletedFor,
