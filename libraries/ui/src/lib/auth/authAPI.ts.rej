diff a/libraries/ui/src/lib/auth/authAPI.ts b/libraries/ui/src/lib/auth/authAPI.ts	(rejected hunks)
@@ -55,10 +55,11 @@ export default function authAPI<T>(endpoint, method = 'GET', options?): Promise<
             .then((thing) => resolve(thing))
             .catch((e) => resolve(e))
         } else {
+          const textBackup = res.clone()
           res.json()
             .then((thing) => reject(thing))
             .catch(() => {
-              res.text()
+              textBackup.text()
                 .then((msg) => reject(msg))
                 .catch(() => reject(res.statusText))
             })
