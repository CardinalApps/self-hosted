diff a/apps/admin-web/src/pages/Roles/CapabilitiesList.tsx b/apps/admin-web/src/pages/Roles/CapabilitiesList.tsx	(rejected hunks)
@@ -16,11 +16,12 @@ import List from '@cardinalapps/ui/src/components/interaction/List'
 import CardGrid from '@cardinalapps/ui/src/components/layout/CardGrid'
 import { ListItem } from '@cardinalapps/ui/src/components/interaction/List/List'
 import Select from '@cardinalapps/ui/src/components/forms/Select'
+import Form from '@cardinalapps/ui/src/components/forms/Form'
+import { pluralize } from '@cardinalapps/ui/src/lib/formatting/text'
 
 import { settingsSelectors } from '@cardinalapps/ui/src/store/slices/settings'
 
 import i18n from './i18n.json'
-import Form from '@cardinalapps/ui/src/components/forms/Form'
 
 type CapabilitiesListProps = {
   globallySelectedComponent: ComponentNames,
@@ -39,6 +40,12 @@ function CapabilitiesList({
   const [filteredItems, setFilteredItems] = useState([])
 
   const filteredString = () => {
+    const pluralizedCapability = pluralize(
+      filteredItems.length,
+      i18n['caps.singular'][lang],
+      i18n['caps.plural'][lang],
+    )
+
     if (!globallySelectedComponent && !globallySelectedRole) {
       return i18n['caps-list.total'][lang]
         .replace('{total}', Capabilities.length)
@@ -49,6 +56,7 @@ function CapabilitiesList({
         .replace('{role}', ACi18n[`role.${globallySelectedRole}.name`]?.[lang])
         .replace('{num}', filteredItems.length)
         .replace('{total}', Capabilities.length)
+        .replace('{capabilities}', pluralizedCapability)
     }
 
     if (globallySelectedComponent && !globallySelectedRole) {
@@ -56,6 +64,7 @@ function CapabilitiesList({
         .replace('{component}', globallySelectedComponent)
         .replace('{num}', filteredItems.length)
         .replace('{total}', Capabilities.length)
+        .replace('{capabilities}', pluralizedCapability)
     }
 
     if (globallySelectedComponent && globallySelectedRole) {
@@ -64,6 +73,7 @@ function CapabilitiesList({
         .replace('{component}', globallySelectedComponent)
         .replace('{num}', filteredItems.length)
         .replace('{total}', Capabilities.length)
+        .replace('{capabilities}', pluralizedCapability)
     }
   }
 
@@ -96,30 +106,17 @@ function CapabilitiesList({
 
   return (
     <CardGrid.Card
-      size="xl"
+      size="l"
       className={'capabilities-list'}
       header={<H5 className={'title'}>{i18n['caps-list.title'][lang]}</H5>}
       footer={filteredString()}
       headerRight={(
         <Form>
           <>
-            <Select
-              name="component"
-              size='s'
-              multi={false}
-              selectPlaceholder={i18n['caps-list.component-filter'][lang]}
-              value={globallySelectedComponent}
-              options={Components.map((component) => {
-                return {
-                  label: component,
-                  value: component,
-                }
-              })}
-              onChange={(selected) => setGloballySelectedComponent(selected)}
-            />
             <Select
               name="role"
               size='s'
+              layout="underline"
               multi={false}
               selectPlaceholder={i18n['caps-list.role-filter'][lang]}
               value={globallySelectedRole}
@@ -131,6 +128,21 @@ function CapabilitiesList({
               })}
               onChange={(selected) => setGloballySelectedRole(selected)}
             />
+            <Select
+              name="component"
+              size='s'
+              layout="underline"
+              multi={false}
+              selectPlaceholder={i18n['caps-list.component-filter'][lang]}
+              value={globallySelectedComponent}
+              options={Components.map((component) => {
+                return {
+                  label: component,
+                  value: component,
+                }
+              })}
+              onChange={(selected) => setGloballySelectedComponent(selected)}
+            />
           </>
         </Form>
       )}
