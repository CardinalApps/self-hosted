diff a/apps/admin-web/src/pages/Users/Users.tsx b/apps/admin-web/src/pages/Users/Users.tsx	(rejected hunks)
@@ -116,10 +116,14 @@ function Users() {
       return []
     }
 
-    const allUsersExceptGuest = users.filter((user: UserType) => user.designation !== 'guest_account')
+    const allUsersExceptGuest = users
+      .filter((user: UserType) => user.designation !== 'guest_account')
+      .filter((user: UserType) => user.role !== 'owner')
     const guestAccount = users.find((user: UserType) => user.designation === 'guest_account')
+    const ownerAccount = users.find((user: UserType) => user.role === 'owner')
 
     const ordered = [
+      ownerAccount,
       ...allUsersExceptGuest,
       guestAccount,
     ]
