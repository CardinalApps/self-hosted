diff a/apps/music-web/src/pages/Tracks/TracksInfiniteScroll.tsx b/apps/music-web/src/pages/Tracks/TracksInfiniteScroll.tsx	(rejected hunks)
@@ -51,15 +51,11 @@ function TracksInfiniteScroll({
     { initialPageParam },
   )
 
-  const { pages = [], pageParams = [] } = data || {}
-  const cachedRTKData = pages.flatMap((res) => res[0])
-  const trailingPageParams = pageParams[0]
-
-  const filter = (musicTrack: MusicTrackType, key) => {
+  const getItem = (musicTrack: MusicTrackType) => {
     const artwork = musicTrack?.release?.thumbnails?.find?.((t) => t.size === 'small_nocrop')?.relativeSrc
     return (
       <MusicTrack
-        key={`item-${key}`}
+        key={`item-${musicTrack?.musicTrackId}`}
         musicTrackId={musicTrack?.id}
         trackTitle={musicTrack?.title}
         releaseTitle={musicTrack?.release?.title}
@@ -69,17 +65,13 @@ function TracksInfiniteScroll({
     )
   }
 
-  const getRowItems = (rowIndex: number) => {
-    return filter(cachedRTKData[rowIndex - trailingPageParams.skip], rowIndex)
-  }
-
   return (
     <VirtualLayout
       virtualViewName={virtualViewName}
       itemHeight={ITEM_HEIGHT}
       itemWidth={ITEM_WIDTH}
       data={data}
-      getRowItems={getRowItems}
+      getItem={getItem}
       isSuccess={isSuccess}
       hasNextPage={hasNextPage}
       hasPreviousPage={hasPreviousPage}
